import type { VideoContent } from '@/types';
import { demoVideoContent } from '@/data/defaults';

/**
 * Default content for the ProductAdvertisement template.
 * Reuses the global demo content — a SaaS product launch.
 */
export const productAdDefaultContent: VideoContent = demoVideoContent;